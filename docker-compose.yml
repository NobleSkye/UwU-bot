services:
  uwu-bot:
    build: .
    container_name: uwu-bot
    restart: unless-stopped
    env_file:
      - .env
    # No ports needed since this is a Discord bot, not a web server
    networks:
      - uwu-bot-network

networks:
  uwu-bot-network:
    driver: bridge